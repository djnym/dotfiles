ARCH=`uname`
if test "$ARCH" = "Linux" ; then
  SETUP=(bashagent prompt screen ssh ww-env-api)
else
  SETUP=(bashagent prompt screen ww-env-api)
fi

for f in $SETUP ; do
  . ${HOME}/.zsh/functions/$f
done
